package tests.M4;

import model.Crop;
import model.CropStage;
import org.junit.Test;
import static org.junit.Assert.assertEquals;

public class SebastianM4Test {
//    @Test
//    public void testCropGrow() {
//        //normal crop life cycle
//        Crop crop = new Crop("Pumpkin", "Apprentice", CropStage.SEED);
//        assertEquals(CropStage.SEED, crop.getStage());
//        assertEquals(50, (int) crop.getWaterLevel());
//        crop.grow();
//        assertEquals(CropStage.IMMATURE, crop.getStage());
//        assertEquals(20, (int) crop.getWaterLevel());
//        crop.setWaterLevel(100);
//        crop.grow();
//        assertEquals(CropStage.MATURE, crop.getStage());
//        assertEquals(70, (int) crop.getWaterLevel());
//        crop.grow();
//        assertEquals(CropStage.DEAD, crop.getStage());
//        assertEquals(40, (int) crop.getWaterLevel());
//
//        //crop should die when waterLevel goes below 0
//        crop.setStage(CropStage.SEED);
//        crop.setWaterLevel(30);
//        crop.grow();
//        assertEquals(CropStage.DEAD, crop.getStage());
//        assertEquals(0, (int) crop.getWaterLevel());
//
//        crop.setStage(CropStage.SEED);
//        crop.setWaterLevel(1);
//        crop.grow();
//        assertEquals(CropStage.DEAD, crop.getStage());
//        assertEquals(0, (int) crop.getWaterLevel());
//    }

}
